{
  "version": 2,
  "builds": [
    { "src": "server/api-server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/socket.io/(.*)", "dest": "server/api-server.js" },
    { "src": "/(.*)", "dest": "api-server.js" }
  ]
}